# Apparently the program is working doesn't mean that the program is good to go.
# We have to perform some tests of input methodologically to make sure it doesn't crash
Therefore we create a file called test_[moduleName].py
In this case it is test_calculator.py

By convention the function inside this program will be named as test_[functionName]
We then call the function with some basic condition 
